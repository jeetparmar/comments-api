server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    display-request-duration: true

spring:
  application:
    name: comment-api
  security:
    user:
      name: swagger
      password: swagger123
  mongodb:
    host: localhost
    port: 27017
    database: comment-db
